name: test

on: [push, pull_request]

jobs:
  test:
    name: Node.js ${{ matrix.node-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        node-version: ['24.15.0', '26.1.0']
        os: [ubuntu-latest, macos-14, windows-latest]
    steps:
      - uses: actions/checkout@v5

      # Source-build fallback dependencies (the install script tries the
      # GitHub Release download first; this is only exercised if that
      # fails — but we want to make sure the fallback compiles cleanly).
      - if: contains(matrix.os, 'macos')
        run: brew install --quiet automake
      - if: contains(matrix.os, 'ubuntu')
        run: sudo apt-get update -qq && sudo apt-get install -qq -y autoconf automake libtool make

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}

      - run: npm install
      - run: npm test
